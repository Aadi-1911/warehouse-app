# Backend API origin. Only VITE_-prefixed variables reach browser code — never put a secret
# here, since anything in this file ends up readable in the shipped JS bundle.
VITE_API_BASE_URL=http://localhost:3001
